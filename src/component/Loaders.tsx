import React from 'react';
import Loader from 'react-loader-spinner';

const Loaders = () => {
  return (
    <div>
      <Loader type="TailSpin" color="#0362fc" height={80} width={80} />
    </div>
  );
};

export default Loaders;
